#ifndef REPLACE_HPP
# define REPLACE_HPP

#include <iostream>
#include <fstream>
#include <sstream>
#include <string>

void    replaceString(std::string& content, const std::string& toFind, const std::string& toReplace);

#endif